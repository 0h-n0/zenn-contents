---
title: "Gemini 3.1 Pro マルチモーダルAPI実践ガイド：画像・音声・動画をPythonで統合処理する"
emoji: "🌟"
type: "tech"
topics: ["gemini", "multimodal", "python", "googleai", "llm"]
published: false
---

# Gemini 3.1 Pro マルチモーダルAPI実践ガイド：画像・音声・動画をPythonで統合処理する

## この記事でわかること

- Gemini 3.1 Pro（2026年2月19日リリース）の全体像と、旧モデル（2.0/2.5）からの実質的な変更点
- `google-genai` SDK を使った画像・音声・動画処理の実装方法
- 新パラメータ `thinking_level`・`media_resolution` の使い分けとコスト最適化
- Gemini 2.5 からの移行チェックリスト（モデルID変更以外の注意点）
- Gemini 3.x ファミリー（3 Flash / 3 Pro / 3.1 Pro）の選定基準と料金比較

## 対象読者

- **想定読者**: LLM APIの実用経験がある中級Python開発者
- **前提知識**: `async/await`の基礎、REST APIの概念、Gemini 2.x の基本的な使用経験

## 結論・成果

Gemini 3.1 Proは2026年2月19日にPreviewリリースされ、ARC-AGI-2ベンチマークで77.1%を達成（Gemini 3 Pro 比で推論性能2倍超）しました（[Google公式ブログ](https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-pro/) より）。

マルチモーダル面では**入力モダリティ（テキスト・画像・動画・音声・PDF）を1つのAPIで統合処理**できる点は2.x系から継承しつつ、新たに導入された `thinking_level` パラメータにより推論深度をタスクに応じて柔軟に制御できるようになっています。

ただし、**音声生成・画像生成・Live API は Gemini 3.1 Pro 非対応**です。これらが必要な場合は Gemini 2.5 Flash や専用モデルを継続使用する必要があります。本記事ではその使い分けも含めて解説します。

---

## Gemini 3.x ファミリーの全体像

### モデル選定マトリクス

2026年2月時点で利用可能な Gemini 3.x ファミリーは以下の通りです（[Gemini API公式ドキュメント](https://ai.google.dev/gemini-api/docs/models) より）。

| モデル | API Model ID | 状態 | 入力価格（≤200k） | 出力価格 | 主な用途 |
|--------|--------------|------|-----------------|----------|----------|
| Gemini 3.1 Pro | `gemini-3.1-pro-preview` | Preview | $2.00/Mトークン | $12.00/M | 複雑推論・エージェント |
| Gemini 3 Pro | `gemini-3-pro-preview` | Preview | $2.00/M | $12.00/M | 高精度マルチモーダル |
| Gemini 3 Flash | `gemini-3-flash-preview` | Preview | $0.50/M | $3.00/M | 汎用・低コスト |

**比較対象（Gemini 2.5系・安定版）**:

| モデル | API Model ID | 状態 | 入力価格 | 特記 |
|--------|--------------|------|----------|------|
| Gemini 2.5 Flash | `gemini-2.5-flash` | GA（安定版） | $0.10/M | 音声生成・TTS・Live API対応 |
| Gemini 2.5 Pro | `gemini-2.5-pro` | GA | $1.25/M | 高精度、本番運用向け |

### 3.1 Pro を選ぶべき場面

Gemini 3.1 Pro は**ソフトウェアエンジニアリングとエージェント型ワークフロー向けに最適化**されており（[公式ドキュメント](https://ai.google.dev/gemini-api/docs/models/gemini-3.1-pro-preview) より）、以下のユースケースで特に有効です。

- **複雑な多段階推論**: コードリポジトリ全体を1コンテキストに収めた解析
- **信頼性の高いツール呼び出し**: カスタムツール対応版 `gemini-3.1-pro-preview-customtools` も提供
- **大規模ドキュメント処理**: 100万トークンコンテキストで長文PDF・動画を一括処理

**3.1 Pro を選ばないケース**:
> 音声合成（TTS）、ネイティブ画像生成、リアルタイム音声対話（Live API）が必要な場合は Gemini 2.5 Flash を継続使用してください。3.1 Pro はこれらの出力モダリティに対応していません。

---

## 環境構築

```bash
pip install google-genai
export GOOGLE_API_KEY="your-api-key"
```

```python
from google import genai
from google.genai import types

client = genai.Client()
```

Gemini 3.x で変わった点として、**APIキーは環境変数 `GOOGLE_API_KEY` から自動読み込み**されるため、コード内でキーを指定する必要はありません（`genai.Client()` の引数省略可）。

---

## 新パラメータ：thinking_level の使い方

Gemini 3.x で最も重要な新機能が `thinking_level` です。推論の深さをタスクに応じて制御でき、コストとレイテンシのトレードオフを調整できます。

### thinking_level の3段階

| 設定値 | 推論深度 | 用途 | コスト感 |
|--------|---------|------|----------|
| `HIGH` | 深い（デフォルト） | 複雑なコード生成・多段階分析 | 高い |
| `MEDIUM` | 中程度 | バランス型・汎用処理 | 中程度 |
| `LOW` | 浅い | 単純な分類・抽出タスク | 低い |

```python
# thinking_level を明示指定する例
def generate_with_thinking(prompt: str, level: str = "HIGH") -> str:
    """thinking_level を指定してテキスト生成する"""
    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=prompt,
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(
                thinking_level=level,
            )
        ),
    )
    return response.text


# 単純な分類タスク → LOW で十分
label = generate_with_thinking(
    "このテキストはポジティブ/ネガティブ/中立のどれですか？: 素晴らしい製品でした",
    level="LOW"
)

# 複雑なコード解析 → HIGH を使用
analysis = generate_with_thinking(
    "このコードのセキュリティ脆弱性を網羅的に分析してください: ...",
    level="HIGH"
)
```

**重要な注意点**:
> Gemini 3.x のデフォルト温度は 1.0 で最適化されています。Gemini 2.x のように `temperature=0.2` などを指定すると、推論タスクでループや品質低下が発生する場合があります。`thinking_level` で制御し、温度の明示指定は基本的に避けてください（[Gemini 3 Developer Guide](https://ai.google.dev/gemini-api/docs/gemini-3) より）。

---

## 画像理解を実装する

### 基本的な画像理解

```python
# image_understanding.py
import mimetypes
from pathlib import Path

from google import genai
from google.genai import types


def analyze_image(image_path: str, prompt: str, thinking_level: str = "MEDIUM") -> str:
    """画像を分析してテキストで結果を返す

    Args:
        image_path: 画像ファイルのパス（JPEG/PNG/WebP/GIF等）
        prompt: 画像に対する質問・指示
        thinking_level: HIGH/MEDIUM/LOW（デフォルト: MEDIUM）

    Returns:
        分析結果のテキスト
    """
    client = genai.Client()
    image_bytes = Path(image_path).read_bytes()
    mime_type, _ = mimetypes.guess_type(image_path)
    if mime_type is None:
        mime_type = "image/jpeg"

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            types.Part.from_bytes(data=image_bytes, mime_type=mime_type),
            prompt,
        ],
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(thinking_level=thinking_level),
        ),
    )
    return response.text


if __name__ == "__main__":
    result = analyze_image(
        "dashboard_screenshot.png",
        "このダッシュボードの異常値や問題点を指摘してください。",
        thinking_level="HIGH",
    )
    print(result)
```

### media_resolution による精度とコストの調整

Gemini 3.x で新たに導入された `media_resolution` パラメータにより、画像処理の解像度（＝トークン消費量）を制御できます。

```python
def analyze_image_with_resolution(
    image_path: str,
    prompt: str,
    resolution: str = "MEDIA_RESOLUTION_MEDIUM",
) -> str:
    """解像度指定付きの画像分析

    resolution の選択基準:
    - HIGH: 細かいテキスト・小さいオブジェクトの識別が必要な場合
    - MEDIUM: 一般的な画像理解（デフォルト）
    - LOW: 全体的なシーン把握のみ、コスト優先の場合
    """
    client = genai.Client()
    image_bytes = Path(image_path).read_bytes()
    mime_type, _ = mimetypes.guess_type(image_path)

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            types.Part.from_bytes(data=image_bytes, mime_type=mime_type or "image/jpeg"),
            prompt,
        ],
        config=types.GenerateContentConfig(
            media_resolution=getattr(types.MediaResolution, resolution),
        ),
    )
    return response.text
```

**トークン消費の目安**:

| 解像度 | トークン/画像 | 用途 |
|--------|-------------|------|
| HIGH | 最大2,000トークン | OCR・設計図・細字のテキスト |
| MEDIUM | 約770トークン（デフォルト） | 一般的な画像分析 |
| LOW | 約258トークン | 全体把握・分類 |

### 20MB 超の画像は Files API を使用

```python
def analyze_large_image(image_path: str, prompt: str) -> str:
    """Files API 経由での大きな画像の分析（20MB超向け）"""
    client = genai.Client()
    uploaded = client.files.upload(file=image_path)

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[uploaded, prompt],
    )
    return response.text
```

---

## 音声処理を実装する（入力のみ）

Gemini 3.1 Pro は**音声入力（理解・文字起こし）には対応していますが、音声出力（TTS）には対応していません**。TTS が必要な場合は `gemini-2.5-flash-preview-tts` を使用してください。

### 音声ファイルの文字起こし・分析

```python
# audio_understanding.py
import mimetypes
from pathlib import Path

from google import genai
from google.genai import types


AUDIO_MIME_TYPES = {
    ".mp3": "audio/mp3",
    ".wav": "audio/wav",
    ".flac": "audio/flac",
    ".aac": "audio/aac",
    ".ogg": "audio/ogg",
    ".m4a": "audio/mp4",
}


def transcribe_audio(audio_path: str) -> str:
    """音声ファイルを文字起こしする（Gemini 3.1 Pro）"""
    client = genai.Client()
    suffix = Path(audio_path).suffix.lower()
    mime_type = AUDIO_MIME_TYPES.get(suffix, "audio/mp3")
    audio_bytes = Path(audio_path).read_bytes()

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            types.Part.from_bytes(data=audio_bytes, mime_type=mime_type),
            "この音声を日本語で文字起こしし、話者が複数の場合は区別してください。",
        ],
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(thinking_level="LOW"),  # 文字起こしはLOWで十分
        ),
    )
    return response.text


def analyze_audio_content(audio_path: str, prompt: str) -> str:
    """音声の内容を深く分析する（感情・意図・背景音等）"""
    client = genai.Client()
    suffix = Path(audio_path).suffix.lower()
    mime_type = AUDIO_MIME_TYPES.get(suffix, "audio/mp3")

    # 長い音声はFiles APIを使用
    uploaded = client.files.upload(file=audio_path)

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[uploaded, prompt],
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(thinking_level="HIGH"),
        ),
    )
    return response.text


if __name__ == "__main__":
    transcript = transcribe_audio("meeting_recording.mp3")
    print(transcript)
```

**トークン消費の目安**: 音声は約32トークン/秒で処理されます。1時間の音声で約115,200トークン（$0.23 at $2/Mトークン）。

---

## 動画理解を実装する

Gemini 3.x は**YouTube URLの直接入力**が新たにサポートされました（公開動画のみ）。

### ファイルアップロードによる動画分析

```python
# video_understanding.py
import time
from google import genai
from google.genai import types


def summarize_video(video_path: str) -> str:
    """動画の内容を要約する（Files API使用）"""
    client = genai.Client()

    # アップロード後、処理完了を待機
    video_file = client.files.upload(file=video_path)

    # ACTIVE状態になるまで待機（大きなファイルでは数分かかる場合あり）
    while video_file.state.name == "PROCESSING":
        time.sleep(5)
        video_file = client.files.get(name=video_file.name)

    if video_file.state.name != "ACTIVE":
        raise RuntimeError(f"動画処理失敗: {video_file.state.name}")

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            video_file,
            "この動画の内容を以下の構成で要約してください：\n"
            "1. 主なトピック（1-3行）\n"
            "2. キーポイント（箇条書き5点以内）\n"
            "3. 特記事項（技術的な内容・注意点）",
        ],
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(thinking_level="HIGH"),
            media_resolution=types.MediaResolution.MEDIA_RESOLUTION_MEDIUM,
        ),
    )
    return response.text


def analyze_video_segment(
    video_path: str,
    start_offset_seconds: int,
    end_offset_seconds: int,
    prompt: str,
) -> str:
    """動画の特定区間を分析する"""
    from google.protobuf.duration_pb2 import Duration

    client = genai.Client()
    video_file = client.files.upload(file=video_path)

    while video_file.state.name == "PROCESSING":
        time.sleep(5)
        video_file = client.files.get(name=video_file.name)

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            types.Content(
                parts=[
                    types.Part(
                        file_data=types.FileData(
                            file_uri=video_file.uri,
                            mime_type=video_file.mime_type,
                        ),
                        video_metadata=types.VideoMetadata(
                            start_offset=Duration(seconds=start_offset_seconds),
                            end_offset=Duration(seconds=end_offset_seconds),
                        ),
                    ),
                    types.Part(text=prompt),
                ]
            )
        ],
    )
    return response.text
```

### YouTube URL から直接分析する

Gemini 3.x の新機能で、公開 YouTube 動画を URL で直接入力できます（Files API 不要）。

```python
def analyze_youtube_video(youtube_url: str, prompt: str) -> str:
    """YouTube動画を直接URLで分析する（Gemini 3.x 新機能）

    Args:
        youtube_url: 公開YouTubeのURL（例: https://www.youtube.com/watch?v=xxxxx）
        prompt: 動画に対する質問・指示

    注意:
        - 公開動画のみ対応（限定公開・非公開は不可）
        - 無料枠では1日8時間まで
    """
    client = genai.Client()

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            types.Part(
                file_data=types.FileData(file_uri=youtube_url)
            ),
            prompt,
        ],
    )
    return response.text


if __name__ == "__main__":
    result = analyze_youtube_video(
        "https://www.youtube.com/watch?v=example",
        "この動画で紹介されている技術的なアーキテクチャを図解で説明してください。",
    )
    print(result)
```

**動画処理のトークン消費**:

| 解像度設定 | トークン/秒 | 1分コスト（$2/M） | 10分コスト |
|-----------|------------|-----------------|-----------|
| デフォルト | 約300 | 約$0.036 | 約$0.36 |
| 低解像度 | 約100 | 約$0.012 | 約$0.12 |

---

## マルチモーダル統合：複数入力の組み合わせ

Gemini 3.1 Pro の真価は、複数のモダリティを**1回のAPIコールで組み合わせる**点にあります。

```python
def analyze_video_with_reference_image(
    video_path: str,
    reference_image_path: str,
    prompt: str,
) -> str:
    """動画と参照画像を組み合わせて分析する

    ユースケース例:
    - 製品動画と設計図を照合して品質チェック
    - プレゼン動画とスライドの整合性確認
    """
    client = genai.Client()
    video_file = client.files.upload(file=video_path)

    while video_file.state.name == "PROCESSING":
        time.sleep(5)
        video_file = client.files.get(name=video_file.name)

    from pathlib import Path
    import mimetypes

    img_bytes = Path(reference_image_path).read_bytes()
    img_mime, _ = mimetypes.guess_type(reference_image_path)

    response = client.models.generate_content(
        model="gemini-3.1-pro-preview",
        contents=[
            video_file,
            types.Part.from_bytes(data=img_bytes, mime_type=img_mime or "image/jpeg"),
            prompt,
        ],
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(thinking_level="HIGH"),
        ),
    )
    return response.text
```

---

## Gemini 2.5 から 3.x への移行ガイド

### 変更が必要な項目

**1. モデルIDの変更（最低限必要）**

```python
# 変更前
model = "gemini-2.5-flash"
model = "gemini-2.5-pro"

# 変更後
model = "gemini-3-flash-preview"   # 低コスト・汎用
model = "gemini-3.1-pro-preview"   # 高精度・エージェント
```

**2. 温度設定の削除（推奨）**

```python
# 変更前（Gemini 2.x スタイル）
config = types.GenerateContentConfig(temperature=0.2)

# 変更後（Gemini 3.x スタイル）
config = types.GenerateContentConfig(
    thinking_config=types.ThinkingConfig(thinking_level="LOW")
    # temperature の明示指定は基本不要
)
```

**3. thinking_level の追加（推奨）**

```python
# タスクの複雑さに応じて thinking_level を設定
simple_tasks = ["分類", "抽出", "文字起こし"]  # → LOW
medium_tasks = ["要約", "翻訳", "QA"]          # → MEDIUM
complex_tasks = ["推論", "コード生成", "分析"] # → HIGH
```

### 変更不要な項目

- `types.Part.from_bytes()` などの基本的なAPI呼び出しパターン
- Files API の使い方
- `types.GenerateContentConfig` の基本構造
- `google-genai` SDK のインポート

### 移行してはいけないケース

以下の機能を使っている場合は **Gemini 2.5 系を継続使用**してください。

| 機能 | 継続使用モデル |
|------|--------------|
| テキスト音声変換（TTS） | `gemini-2.5-flash-preview-tts` |
| ネイティブ画像生成 | `gemini-2.0-flash-exp`（実験版）または Imagen 3 |
| リアルタイム音声対話（Live API） | `gemini-2.0-flash-live-001` |
| 本番環境での安定稼働 | `gemini-2.5-flash`（GA版）、`gemini-2.5-pro`（GA版） |

> Gemini 3.x は2026年2月時点で全モデル **Preview** ステータスです。本番環境での SLA が必要な場合は、GA 版の Gemini 2.5 Flash/Pro を使用してください。

---

## コスト最適化の実践

### 4段階のコスト削減戦略

**戦略1: モデルを使い分ける**

```python
def route_request(task_type: str, content: str) -> str:
    """タスクの複雑さに応じてモデルを切り替える"""
    if task_type in ("classification", "extraction", "transcription"):
        # 単純タスク → 3 Flash（$0.50/M）
        model = "gemini-3-flash-preview"
        thinking = "LOW"
    elif task_type in ("summarization", "qa", "translation"):
        # 中程度 → 3 Flash + MEDIUM
        model = "gemini-3-flash-preview"
        thinking = "MEDIUM"
    else:
        # 複雑推論 → 3.1 Pro（$2.00/M）
        model = "gemini-3.1-pro-preview"
        thinking = "HIGH"

    client = genai.Client()
    response = client.models.generate_content(
        model=model,
        contents=content,
        config=types.GenerateContentConfig(
            thinking_config=types.ThinkingConfig(thinking_level=thinking),
        ),
    )
    return response.text
```

**戦略2: media_resolution を下げる**

```python
# 動画の全体把握のみ必要な場合
config = types.GenerateContentConfig(
    media_resolution=types.MediaResolution.MEDIA_RESOLUTION_LOW,
    # トークン消費を約1/3に削減（300→100トークン/秒）
)
```

**戦略3: 動画クリッピング**

```python
from google.protobuf.duration_pb2 import Duration

# 必要な区間のみ処理（全体ではなく30秒のみ）
video_metadata = types.VideoMetadata(
    start_offset=Duration(seconds=60),   # 1分からスタート
    end_offset=Duration(seconds=90),     # 1分30秒まで
)
```

**戦略4: Files API でファイルを再利用**

```python
# アップロードしたファイルは48時間保持される
# 同じファイルへの複数の質問はアップロードを1回に抑える

video_file = client.files.upload(file="long_lecture.mp4")

questions = [
    "この講義の主なトピックは何ですか？",
    "第2章の要点をまとめてください。",
    "講師が強調していたポイントを抽出してください。",
]

for question in questions:
    response = client.models.generate_content(
        model="gemini-3-flash-preview",
        contents=[video_file, question],
    )
    print(f"Q: {question}\nA: {response.text}\n")
```

### コスト試算例

**10分の会議動画を処理する場合（デフォルト解像度）**:

```
動画入力トークン: 300トークン/秒 × 600秒 = 180,000トークン
料金: 180,000 / 1,000,000 × $0.50 (3 Flash) = $0.09（約13円）
または: 180,000 / 1,000,000 × $2.00 (3.1 Pro) = $0.36（約53円）
```

低解像度を使うと3 Flashで約$0.03（4.5円）まで削減可能です。

---

## よくある問題と解決方法

| 問題 | 原因 | 解決方法 |
|------|------|----------|
| `FileNotFoundError` (Files API) | アップロード後すぐにリクエスト | `state == "ACTIVE"` を確認してからリクエスト |
| 推論が遅い | `thinking_level="HIGH"` + 大量トークン | 単純タスクは `thinking_level="LOW"` に変更 |
| 出力が不安定 | `temperature` を明示指定している | `temperature` 指定を削除し `thinking_level` で制御 |
| YouTube URLエラー | 限定公開・非公開動画 | 公開動画のみ対応。非公開はFiles API経由で処理 |
| `InvalidArgument` | モデルが非対応機能を要求 | 3.1 Proは音声/画像生成非対応。2.5 Flashを使用 |
| `ResourceExhausted` | レート制限超過 | 指数バックオフ付きリトライを実装 |

### リトライ実装のテンプレート

```python
import time
import random
from google.api_core.exceptions import ResourceExhausted


def generate_with_retry(
    client: genai.Client,
    model: str,
    contents,
    max_retries: int = 3,
) -> str:
    """指数バックオフ付きリトライ"""
    for attempt in range(max_retries):
        try:
            response = client.models.generate_content(
                model=model,
                contents=contents,
            )
            return response.text
        except ResourceExhausted:
            if attempt == max_retries - 1:
                raise
            wait = (2 ** attempt) + random.uniform(0, 1)
            time.sleep(wait)
    return ""
```

---

## まとめと次のステップ

**まとめ**:

- Gemini 3.1 Pro（`gemini-3.1-pro-preview`）は2026年2月19日リリース。ARC-AGI-2で77.1%を達成し、複雑推論・エージェントワークフローに最適
- 入力モダリティ（テキスト・画像・動画・音声・PDF）は2.x系から継承。**音声生成・画像生成・Live APIは非対応**
- 新パラメータ `thinking_level`（HIGH/MEDIUM/LOW）でコストとレイテンシをタスクに応じて制御可能
- `media_resolution` パラメータで動画・画像のトークン消費を最大1/3に削減可能
- YouTube URL 直接入力が新たに対応（公開動画のみ）
- Gemini 3.x は全モデル**Preview**ステータス。本番SLAが必要な場合は GA の Gemini 2.5 Flash/Pro を使用

**次にやるべきこと**:

1. [Google AI Studio](https://aistudio.google.com/) で `gemini-3.1-pro-preview` を試し、既存の `gemini-2.5-flash` プロンプトとの出力差異を確認する
2. `thinking_level="LOW"` → `"HIGH"` の違いをコスト重視タスクで計測し、最適な設定を見極める
3. GA（安定版）リリースのタイミングを追い、本番移行の計画を立てる

**関連記事**:
- [Gemini 3.1 Proで構築するマルチエージェント協調コーディングの実践手法](https://zenn.dev/0h_n0/articles/a7935e0412571c)

## 参考

- [Gemini 3.1 Pro: A smarter model - Google Blog](https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-pro/)
- [Gemini 3.1 Pro Preview - API公式ドキュメント](https://ai.google.dev/gemini-api/docs/models/gemini-3.1-pro-preview)
- [Gemini 3 Developer Guide](https://ai.google.dev/gemini-api/docs/gemini-3)
- [Video Understanding - Gemini API](https://ai.google.dev/gemini-api/docs/video-understanding)
- [Models | Gemini API](https://ai.google.dev/gemini-api/docs/models)
- [Gemini API Pricing](https://ai.google.dev/gemini-api/docs/pricing)
- [Gemini 3.1 Pro on Vertex AI - Google Cloud Blog](https://cloud.google.com/blog/products/ai-machine-learning/gemini-3-1-pro-on-gemini-cli-gemini-enterprise-and-vertex-ai)
- [Google launches Gemini 3.1 Pro - VentureBeat](https://venturebeat.com/technology/google-launches-gemini-3-1-pro-retaking-ai-crown-with-2x-reasoning)

---

:::message
この記事はAI（Claude Code）により自動生成されました。内容の正確性については複数の情報源で検証していますが、実際の利用時は公式ドキュメントもご確認ください。
:::
